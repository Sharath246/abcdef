name: trigger
on: push
jobs:
  abcd:
    runs-on: ubuntu-latest
    steps:
      - name: run sometjing
        run: | 
          curl -L \
          -X POST \
          -H "Accept: application/vnd.github+json" \
          -H "Authorization: Bearer ${{secrets.CICDTOKEN}}" \
          -H "X-GitHub-Api-Version: 2022-11-28" \
          https://api.github.com/repos/Sharath246/cicd/dispatches \
          -d '{"event_type":"abcd","client_payload":{"unit":false,"integration":true}}'
      - run: echo "completed"
