name: trigger

env:
  type: string

on: 
  workflow_dispatch:
    inputs:
      sele:
        type: choice
        default: adasd
      seleadsas:
        description: werwerwerwerqwe
        type: boolean
      seledasdasd:
        description: aerwerwerwer
        type: boolean
      sdasdasdasdasdasda:
        description: sdfsdfsdf
        type: boolean
jobs:
  abcd:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{github.actor}}"
        
