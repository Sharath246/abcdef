name: trigger
on: 
  workflow_dispatch:
    inputs:
      sele:
        type: boolean
      seleadsas:
        type: boolean
      seledasdasd:
        type: boolean
      sdasdasdasdasdasda:
        type: boolean
jobs:
  abcd:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{github.actor}}"
        
