name: trigger
on: 
  workflow_dispatch:
    inputs:
      sele:
        description: asads
        type: choice
        options:
          - a
      sefsef:
        description: asd
        type: choice
        options:
          - b
      asdasdasdas:
        description: asd
        type: choice
        options:
          - c
jobs:
  abcd:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{github.actor}}"
        
