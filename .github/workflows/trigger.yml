name: trigger
on: 
  workflow_dispatch:
    inputs:
      sele:
        description: asads
        type: choice
        options:
          - a
          - b
          - c
jobs:
  abcd:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{github.actor}}"
        
