name: trigger

on: push
jobs:
  abcd:
    runs-on: ubuntu-latest
    steps:
      - name: custom marker builder
        id: custom-marker
        env:
          MARKERS: "asdjbaskd\n\nasdas\nasdasd"
        run: |
          conv=$(echo "$MARKERS" | tr '\n' '<br>')
          echo "$conv"
        
