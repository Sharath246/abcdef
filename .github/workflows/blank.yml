name: CI test
on: push
jobs:
  Heoll:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          ls
          cd abcdef
          ls
      - name: hello
        id: wew
        run: |
          echo "greeting=hello" >> $GITHUB_OUTPUT
          cat $GITHUB_OUTPUT
          
