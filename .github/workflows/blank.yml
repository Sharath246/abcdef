name: CI test
on: workflow_call
jobs:
  Heoll:
    runs-on: ubuntu-latest
    steps:
      - name: hello
        id: wew
        run: |
          echo "greeting=hello" >> $GITHUB_OUTPUT
          cat $GITHUB_OUTPUT
          
