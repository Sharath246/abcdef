name: CI
on: 
  push:
    branches:
      - main
jobs:
  Heoll:
    runs-on: ubuntu-latest
    steps:
      - uses: peter-evans/repository-dispatch@v3
        with:
          repo: Sharath246/cicd
          event-type: abcd
